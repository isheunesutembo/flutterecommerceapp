import 'package:flutter/material.dart';

class Icon_Widget extends StatelessWidget {
  Icon icon;
  final   Function onPressed;
   Icon_Widget({super.key,required this.icon,required this.onPressed});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}